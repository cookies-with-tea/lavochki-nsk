import styled from '@emotion/styled'

export const StyledAllBenchesLink = styled.a`
    font-size: 22px;
    padding: 12px 22px;
    font-weight: 500;
    color: var(--color--background);
    background-color: var(--color--primary-dark);
    cursor: pointer;
`