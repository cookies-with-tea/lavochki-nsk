import styled from "@emotion/styled";

export const StyledOnTheMap = styled.a`
    font-size: 22px;
    line-height: 30px;
    color: var(--color--primary-dark);
    text-decoration: underline;
    border: none;
  padding: 0;
`