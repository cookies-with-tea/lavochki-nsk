import { SwiperOptions } from 'swiper/types'

const BREAKPOINTS: SwiperOptions['breakpoints'] = {
  1200: {
    slidesPerView: 4,
    spaceBetween: 22
  }
}

export const nearBenchesConfig = {
  BREAKPOINTS,
}
