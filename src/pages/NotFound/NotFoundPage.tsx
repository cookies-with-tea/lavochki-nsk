import React from 'react';
import CommonIcon from "@/components/Common/CommonIcon/CommonIcon";

export const NotFoundPage = () => {
    return (
        <div>
            <CommonIcon name={'404'} width={600} height={500} />
        </div>
    );
};
